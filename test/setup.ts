// Vitest global setup. Intentionally empty for now — boundary fakes are
// injected per-test (Spec 08 §6.1), so there is no global mutable state to
// reset here. Kept as a seam for future test-wide configuration.
export {};
