/**
 * Pinned Kubernetes control-plane version for the envtest harness (Spec 08
 * §5.3). Keep in sync with `scripts/setup-envtest.sh` and the CI workflow.
 */
export const ENVTEST_K8S_VERSION = '1.31.0';
